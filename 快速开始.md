# ⚡ opcode 中文版本 - 快速开始指南

## 🎯 30秒快速启动

### Windows 用户
```bash
# 1. 双击运行脚本
scripts\start-chinese.bat
```

### macOS/Linux 用户
```bash
# 1. 给脚本添加执行权限
chmod +x scripts/start-chinese.sh

# 2. 运行启动脚本
./scripts/start-chinese.sh
```

### 或者使用 npm 命令
```bash
# 一键启动中文版本
npm run start:chinese
```

## 🚀 详细启动步骤

### 第一步：准备工作

#### 1. 确保环境
- ✅ **Node.js** 18+
- ✅ **npm** 9+
- ✅ **Rust** 1.70.0+

#### 2. 检查环境版本
```bash
node --version    # 应该显示 v18.x 或更高
npm --version     # 应该显示 9.x 或更高
rustc --version   # 应该显示 1.70.0 或更高
```

### 第二步：安装依赖

#### 如果首次运行
```bash
# 安装所有依赖
npm install
```

#### 如果已经安装过
```bash
# 更新依赖（可选）
npm update
```

### 第三步：启动应用

#### 推荐方式：使用中文启动脚本
```bash
npm run start:chinese
```

#### 备选方式：直接启动
```bash
npm run tauri dev
```

## 🎨 首次使用指南

### 1. 应用启动后
- 🏠 你会看到中文欢迎界面
- 🌐 右上角有语言切换图标
- 📱 界面完全汉化

### 2. 切换语言
1. 点击右上角的 🌐 图标
2. 选择 "🇨🇳 中文" 或 "🇺🇸 English"
3. 界面会立即切换

### 3. 主要功能
- 🤖 **CC 代理** - AI 代理管理
- 📁 **项目** - 项目管理
- ⚙️ **设置** - 应用设置
- 📊 **用量统计** - 使用数据分析
- 🌐 **MCP 管理** - 模型上下文协议管理

## 🔧 常见问题快速解决

### ❌ 问题："Node.js not found"
**解决**：
1. 下载 Node.js: https://nodejs.org/
2. 安装 LTS 版本
3. 重启终端

### ❌ 问题："Rust not found"
**解决**：
1. 打开终端运行：
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```
2. 重启终端

### ❌ 问题："webkit2gtk not found" (Linux)
**解决**：
```bash
# Ubuntu/Debian
sudo apt install -y libwebkit2gtk-4.1-dev
```

### ❌ 问题：构建失败
**解决**：
```bash
# 清理并重新构建
npm run check
cd src-tauri && cargo clean
npm install
npm run start:chinese
```

## 📋 启动检查清单

### 启动前检查
- ☐ 已安装 Node.js 18+
- ☐ 已安装 Rust 1.70.0+
- ☐ 当前在项目根目录
- ☐ 已安装依赖 (node_modules 存在)

### 启动后验证
- ☐ 应用窗口正常打开
- ☐ 显示中文界面
- ☐ 无错误提示
- ☐ 所有功能模块正常加载

## 🎪 开发模式特性

### 热重载
- 🔄 代码修改后自动刷新
- 💾 保持应用状态
- ⚡ 快速开发迭代

### 调试工具
- 🔍 浏览器开发者工具 (F12)
- ⚛️ React DevTools
- 🦀 Rust 调试输出

## 📞 获取帮助

### 查看帮助文档
```bash
# 详细启动说明
cat 启动说明.md

# 汉化说明
cat 汉化说明.md
```

### 检查项目状态
```bash
# 检查 TypeScript
npm run check

# 查看可用脚本
npm run
```

### 清理项目
```bash
# 清理构建文件
rm -rf node_modules dist
cd src-tauri && cargo clean

# 重新安装
npm install
```

## 🎉 成功标志

当你看到以下内容时，说明启动成功：

✅ **应用窗口正常打开**
✅ **显示中文欢迎界面**
✅ **右上角有 🌐 语言切换图标**
✅ **底部状态栏显示 "就绪"**
✅ **无红色错误提示**

## 🔄 日常开发流程

### 开始开发
```bash
npm run start:chinese
```

### 代码检查
```bash
npm run check
```

### 构建生产版本
```bash
npm run build:chinese
```

### 更新汉化
```bash
# 拉取最新汉化更新
git pull origin chinese-localization
```

---

**💡 提示**: 这是中文汉化版本，所有界面都已汉化，为中文用户提供更好的使用体验。

**🔗 项目地址**: https://github.com/xiaocuanChina/opcode/tree/chinese-localization

**📅 最后更新**: 2024年2月5日