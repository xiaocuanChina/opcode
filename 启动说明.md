# 🚀 opcode 中文版本启动指南

## 📋 系统要求

### 基础环境
- **Node.js**: v18 或更高版本
- **npm**: v9 或更高版本
- **Rust**: 1.70.0 或更高版本
- **Git**: 用于版本控制

### 平台特定要求

#### Windows
- ✅ Microsoft C++ Build Tools
- ✅ WebView2 (Windows 11 通常已预装)

#### macOS
- ✅ Xcode Command Line Tools
- ✅ Homebrew (推荐)

#### Linux (Ubuntu/Debian)
```bash
sudo apt update
sudo apt install -y \
  libwebkit2gtk-4.1-dev \
  libgtk-3-dev \
  libayatana-appindicator3-dev \
  librsvg2-dev \
  patchelf \
  build-essential \
  curl \
  wget \
  file \
  libssl-dev \
  libxdo-dev \
  libsoup-3.0-dev \
  libjavascriptcoregtk-4.1-dev
```

## 🛠️ 安装依赖

### 1. 克隆汉化版本
```bash
# 克隆你的汉化版本
git clone https://github.com/xiaocuanChina/opcode.git
cd opcode

# 切换到汉化分支
git checkout chinese-localization
```

### 2. 安装前端依赖
```bash
# 使用 npm 安装
npm install

# 或者如果你喜欢用 bun
# bun install
```

### 3. 安装 Rust 工具链
```bash
# 如果你还没有安装 Rust
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# 安装完成后，重启终端或运行
source ~/.cargo/env
```

### 4. 安装 Tauri CLI
```bash
# Tauri CLI 会通过 npm 依赖自动安装
# 如果没有，可以手动安装
cargo install tauri-cli
```

## 🎯 启动应用

### 方法一：使用 npm 脚本（推荐）

#### 开发模式启动
```bash
# 启动开发服务器（热重载）
npm run tauri dev
```

#### 构建并运行
```bash
# 构建生产版本
npm run tauri build

# 运行构建后的应用
# Windows: src-tauri/target/release/opcode.exe
# macOS/Linux: src-tauri/target/release/opcode
```

### 方法二：使用 just 命令

#### 安装 just（如果还没有）
```bash
# macOS
brew install just

# Ubuntu/Debian
sudo apt install just

# 或者使用 cargo
cargo install just
```

#### 使用 just 启动
```bash
# 安装依赖并启动开发模式
just dev

# 或者分步执行
just install    # 安装依赖
just build      # 构建项目
just run        # 运行应用
```

### 方法三：手动启动

```bash
# 1. 构建前端
npm run build

# 2. 启动 Tauri 应用
cd src-tauri
cargo run
```

## 🌐 中文语言设置

### 首次启动
1. 应用启动后，默认显示中文界面
2. 点击右上角的 🌐 图标
3. 确认选择 "🇨🇳 中文"

### 切换语言
- 随时可以通过 🌐 图标切换中英文
- 语言偏好会自动保存
- 重启应用后会记住上次选择

## 🔧 常见问题解决

### 问题 1: "cargo not found" 错误
**解决方案**:
```bash
# 确保 Rust 已正确安装
rustc --version
cargo --version

# 如果命令不存在，重新安装 Rust
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source ~/.cargo/env
```

### 问题 2: "webkit2gtk not found" (Linux)
**解决方案**:
```bash
# Ubuntu/Debian
sudo apt install -y libwebkit2gtk-4.1-dev

# 对于较新的 Ubuntu 版本，可能需要
sudo apt install -y libwebkit2gtk-4.0-dev
```

### 问题 3: "MSVC not found" (Windows)
**解决方案**:
1. 下载并安装 [Visual Studio Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/)
2. 安装时选择 "C++ build tools"
3. 重启终端后重试

### 问题 4: 内存不足导致构建失败
**解决方案**:
```bash
# 减少并行编译任务
cargo build -j 2

# 或者关闭其他应用释放内存
```

### 问题 5: "claude command not found"
**解决方案**:
1. 确保已安装 [Claude Code CLI](https://claude.ai/code)
2. 测试安装: `claude --version`
3. 如果命令不存在，重新安装并确保在 PATH 中

## 📊 构建状态检查

### 检查 TypeScript 编译
```bash
npm run check
```

### 检查 Rust 编译
```bash
cd src-tauri
cargo check
```

### 运行测试
```bash
cd src-tauri
cargo test
```

## 🎨 开发模式特性

### 热重载
- ✅ 前端代码修改后自动刷新
- ✅ 保留应用状态
- ✅ 快速开发迭代

### 调试工具
- ✅ 浏览器开发者工具 (F12)
- ✅ React DevTools
- ✅ Rust 调试输出

## 📦 生产构建

### 构建安装包
```bash
# Windows - 构建 MSI 安装包
npm run tauri build

# macOS - 构建 DMG 安装包
npm run build:dmg

# Linux - 构建 AppImage
npm run tauri build
```

### 构建输出位置
- **Windows**: `src-tauri/target/release/opcode.exe`
- **macOS**: `src-tauri/target/release/opcode`
- **Linux**: `src-tauri/target/release/opcode`

## 🆘 获取帮助

### 检查环境
```bash
# 检查 Node.js 版本
node --version

# 检查 npm 版本
npm --version

# 检查 Rust 版本
rustc --version

# 检查 Tauri 版本
cargo tauri --version
```

### 清理和重装
```bash
# 清理所有构建文件
just clean

# 或者手动清理
rm -rf node_modules dist
cd src-tauri && cargo clean

# 重新安装
npm install
```

### 查看日志
```bash
# 开发模式下查看控制台输出
npm run tauri dev

# 查看 Rust 编译日志
cd src-tauri && cargo build --verbose
```

## 🎉 成功启动标志

✅ 应用窗口正常打开
✅ 显示中文欢迎界面
✅ 右上角有 🌐 语言切换图标
✅ 所有功能模块正常加载
✅ 无错误提示信息

---

**维护者**: xiaocuanChina
**最后更新**: 2024年2月5日
**版本**: v0.2.1-chinese