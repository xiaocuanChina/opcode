# 🇨🇳 汉化说明文档

本分支为 opcode 项目提供了完整的中文本地化支持。

## ✨ 功能特性

### 已完成汉化

✅ **核心界面**
- 欢迎页面
- 主导航菜单
- 设置页面
- 项目列表
- 状态栏和提示信息

✅ **基础组件**
- 语言切换组件
- 错误和成功消息
- 按钮和标签文本

✅ **国际化架构**
- 完整的 i18n 架构
- 中英文翻译文件
- 动态语言切换

### 汉化内容

| 模块 | 状态 | 说明 |
|------|------|------|
| 欢迎页面 | ✅ | 主欢迎界面和导航卡片 |
| 设置页面 | ✅ | 设置界面和选项 |
| 项目列表 | ✅ | 项目管理和浏览 |
| 语言切换 | ✅ | 中英文切换功能 |
| 错误消息 | ✅ | 错误提示和成功消息 |
| Topbar | ✅ | 顶部状态栏 |

## 🚀 使用方法

### 切换语言

1. 点击界面右上角的 🌐 图标
2. 选择 "🇨🇳 中文" 或 "🇺🇸 English"
3. 界面将立即切换到对应语言

### 默认语言

- 默认显示中文界面
- 语言偏好会保存在本地存储中
- 下次启动时会自动使用上次选择的语言

## 🔧 技术实现

### 架构设计

- **框架**: 使用 `react-i18next` 实现国际化
- **文件结构**:
  ```
  src/locales/
  ├── zh-CN/
  │   └── translation.ts    # 中文翻译
  ├── en-US/
  │   └── translation.ts    # 英文翻译
  └── index.ts              # 导出配置
  ```

- **翻译格式**: 使用嵌套对象结构，便于组织和维护
- **动态切换**: 支持运行时语言切换，无需重启应用

### 翻译键名规范

```typescript
{
  common: {
    // 通用词汇
  },
  app: {
    // 应用相关
  },
  navigation: {
    // 导航菜单
  },
  settings: {
    // 设置页面
  },
  projects: {
    // 项目管理
  },
  agents: {
    // 代理相关
  },
  errors: {
    // 错误消息
  },
  success: {
    // 成功消息
  }
}
```

## 🔄 同步原项目更新

### 定期同步流程

1. **获取最新更新**:
   ```bash
   git checkout main
   git pull origin main
   ```

2. **合并到汉化分支**:
   ```bash
   git checkout chinese-localization
   git merge main
   ```

3. **处理冲突**:
   - 检查新增的英文文本
   - 添加到翻译文件中
   - 更新对应组件

4. **测试和提交**:
   ```bash
   npm run check
   git add .
   git commit -m "sync: 同步原项目更新并更新翻译"
   git push origin chinese-localization
   ```

### 维护建议

- **定期检查**: 建议每周检查一次原项目更新
- **增量更新**: 只翻译新增或修改的文本
- **保持同步**: 确保功能更新和汉化同步进行

## 📝 待汉化模块

### 优先级排序

1. **高优先级**
   - 代理管理界面 (CC Agents)
   - 用量统计页面
   - MCP 管理界面

2. **中优先级**
   - 编辑器组件
   - 会话管理
   - 工具组件

3. **低优先级**
   - 帮助文档
   - 关于页面
   - 第三方组件

## 🤝 贡献指南

### 添加新翻译

1. **在翻译文件中添加**:
   ```typescript
   // src/locales/zh-CN/translation.ts
   export const zhCN = {
     newModule: {
       newKey: "新翻译内容"
     }
   };
   ```

2. **在组件中使用**:
   ```tsx
   import { useTranslation } from 'react-i18next';

   function MyComponent() {
     const { t } = useTranslation();
     return <div>{t('newModule.newKey')}</div>;
   }
   ```

### 翻译规范

- 保持术语一致性
- 使用标准中文表达
- 注意标点符号（使用中文标点）
- 保持简洁明了

## 🐛 问题反馈

如果遇到汉化相关的问题，请：

1. 检查是否为最新版本的汉化分支
2. 查看是否有遗漏的未汉化文本
3. 在 GitHub Issues 中反馈问题

## 📄 许可证

本项目遵循原项目的 AGPL-3.0 许可证。
汉化工作由社区贡献者完成，与原项目作者无关。

---

**维护者**: xiaocuanChina
**最后更新**: 2024年2月
**版本**: v0.2.1